from setuptools import setup, find_packages

setup(
    name='math_helper',
    version='0.1',
    packages=find_packages(),
    description='A collection of handy math utility functions in Python.',
    author='Your Name',
    install_requires=[],
)
